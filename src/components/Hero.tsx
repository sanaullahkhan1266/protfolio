export default function Hero() {
  return (
    <section id="home" className="relative flex min-h-screen items-center justify-center opacity-0 animate-fade-up [animation-delay:120ms]">
      {/* Intentionally empty to let the 3D cover shine */}
      <div className="sr-only">Fullscreen cover</div>
    </section>
  );
}
